// fazer vetor listaPedido
    // armazenar: itens (precisa ter um jeito de ter vários itens (????????)) (talvez adicionar um atributo na classe cliente e ligar um destructor quando encerrar pedido) 
                  adicionais(com), 
                  ingredientes(sem), 
                  valor do item, 
                  valor do pedido (soma do valor dos itens), 
                  valor total (valor pedido + valor entrega)

// armazenar pedidos em um .txt (além do arquivo pra impressão (pedido-00_cliente-'nomecliente') ) estilo listaClientes.txt
// criar função pra ler .txt e armazenar pedido no vetor listaPedido (estilo função carregarClientes)

// fazer funcao fecharPedidos, que apaga vetor listaPedido, atualiza listaTodosPedidos.txt e apaga arquivos .txt individuais dos pedidos (arquivo pra impressão)
